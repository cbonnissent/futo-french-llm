# Core dependencies (optimisées selon HF Skills)
torch>=2.1.0
transformers>=4.40.0
sentencepiece>=0.1.99
datasets>=2.14.0
accelerate>=0.27.0

# TRL pour SFT/DPO/GRPO (recommandé HF Skills)
trl>=0.8.0

# Optimisations performance
flash-attn>=2.5.0  # Flash Attention 2 (2-3x plus rapide)
bitsandbytes>=0.42.0  # Quantization

# Data processing
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# GGUF conversion
gguf>=0.6.0

# Monitoring & Logging
wandb>=0.16.0  # Weights & Biases
tensorboard>=2.15.0

# Evaluation
evaluate>=0.4.0
scikit-learn>=1.3.0

# HuggingFace Hub integration
huggingface-hub>=0.20.0

# Optional: DPO/GRPO si besoin
# peft>=0.9.0  # Pour LoRA si modèle trop grand
